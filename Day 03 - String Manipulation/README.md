# Day 03 - String Manipulation

## Status
⏳ Planned / Next

## Goal
Practice working with strings (formatting, methods, and common operations).

## What I plan to learn
- f-strings and formatting
- String methods: `.lower()`, `.upper()`, `.strip()`, `.replace()`
- Searching: `in`, `.find()`
- Splitting and joining: `.split()`, `" ".join()`

## Files
- (coming tomorrow)
